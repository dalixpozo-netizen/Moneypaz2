"use client";

import { useState } from "react";
import { 
  Wallet, 
  PlusCircle, 
  Bell, 
  ArrowUpCircle, 
  ArrowDownCircle, 
  Calendar,
  ChevronRight,
  TrendingUp,
  X
} from "lucide-react";

// Definición de tipos para nuestra estructura de datos
interface Alert {
  id: string;
  title: string;
  date: string;
  amount: number;
  type: 'fixed' | 'variable';
}

export default function App() {
  // Estados iniciales para la demo (luego vendrán de Supabase)
  const [balance] = useState(1250.85);
  const [income] = useState(2500.00);
  const [expenses] = useState(1249.15);
  const [isModalOpen, setIsModalOpen] = useState(false);
  
  const [alerts] = useState<Alert[]>([
    { id: '1', title: 'Alquiler Piso', date: '2024-03-01', amount: 850, type: 'fixed' },
    { id: '2', title: 'Suscripción Netflix', date: '2024-03-05', amount: 12.99, type: 'fixed' },
    { id: '3', title: 'Seguro Coche', date: '2024-03-12', amount: 45.50, type: 'variable' }
  ]);

  return (
    <div className="min-h-screen bg-[#0A0C10] text-gray-100 font-sans pb-24">
      {/* Header Superior */}
      <header className="max-w-md mx-auto p-6 flex items-center justify-between sticky top-0 bg-[#0A0C10]/80 backdrop-blur-md z-50">
        <div className="flex items-center gap-2">
          <div className="p-2 bg-emerald-500 rounded-xl shadow-lg shadow-emerald-500/20">
            <Wallet className="w-6 h-6 text-black" strokeWidth={2.5} />
          </div>
          <h1 className="text-2xl font-black tracking-tighter">MONEYPAZ</h1>
        </div>
        <button className="relative p-2 bg-[#161B22] border border-[#30363D] rounded-full hover:bg-[#1c2128] transition-colors">
          <Bell className="w-5 h-5 text-gray-400" />
          <span className="absolute top-1 right-1 w-2.5 h-2.5 bg-blue-500 rounded-full border-2 border-[#0A0C10]"></span>
        </button>
      </header>

      <main className="max-w-md mx-auto px-6 space-y-8 animate-slide-up">
        {/* Tarjeta de Balance Principal */}
        <section className="relative overflow-hidden rounded-[2.5rem] bg-gradient-to-br from-emerald-500 to-emerald-600 p-8 text-black shadow-2xl shadow-emerald-500/30">
          <div className="relative z-10">
            <div className="flex justify-between items-start">
              <div>
                <p className="text-[10px] font-black uppercase tracking-[0.2em] opacity-70">Balance Total</p>
                <h2 className="text-5xl font-black mt-1 tracking-tighter">{balance.toLocaleString()}€</h2>
              </div>
              <div className="bg-black/10 p-2 rounded-lg">
                <TrendingUp className="w-5 h-5" />
              </div>
            </div>
            
            <div className="grid grid-cols-2 gap-4 mt-10">
              <div className="bg-white/20 backdrop-blur-md p-4 rounded-2xl border border-white/10">
                <div className="flex items-center gap-1.5 text-[10px] font-bold uppercase mb-1 opacity-80">
                  <ArrowUpCircle className="w-3.5 h-3.5" /> Ingresos
                </div>
                <p className="text-xl font-black">{income.toLocaleString()}€</p>
              </div>
              <div className="bg-black/5 backdrop-blur-md p-4 rounded-2xl border border-black/5">
                <div className="flex items-center gap-1.5 text-[10px] font-bold uppercase mb-1 opacity-60">
                  <ArrowDownCircle className="w-3.5 h-3.5" /> Gastos
                </div>
                <p className="text-xl font-black">{expenses.toLocaleString()}€</p>
              </div>
            </div>
          </div>
          {/* Decoración circular de fondo */}
          <div className="absolute -top-12 -right-12 w-40 h-40 bg-white/10 rounded-full blur-3xl"></div>
        </section>

        {/* Sección de Alertas Próximas (Tu pedido especial) */}
        <section className="space-y-4">
          <div className="flex items-center justify-between px-1">
            <h3 className="text-lg font-black tracking-tight flex items-center gap-2">
              <Bell className="w-5 h-5 text-blue-400" /> 
              Alertas Próximas
            </h3>
            <span className="text-[10px] font-bold text-blue-400 bg-blue-400/10 px-2 py-1 rounded-full border border-blue-400/20">
              {alerts.length} ACTIVAS
            </span>
          </div>
          
          <div className="space-y-3">
            {alerts.map((alert) => (
              <div key={alert.id} className="bg-[#161B22]/80 backdrop-blur-md border border-[#30363D] p-4 rounded-3xl flex items-center justify-between group cursor-pointer hover:border-emerald-500/50 transition-all duration-300">
                <div className="flex items-center gap-4">
                  <div className="w-12 h-12 bg-blue-500/10 rounded-2xl flex items-center justify-center text-blue-400 group-hover:bg-blue-500/20 transition-colors">
                    <Calendar className="w-6 h-6" />
                  </div>
                  <div>
                    <p className="font-bold text-sm leading-tight">{alert.title}</p>
                    <p className="text-[10px] text-gray-500 font-medium uppercase mt-1 tracking-wider">
                      {new Date(alert.date).toLocaleDateString('es-ES', { day: 'numeric', month: 'long' })}
                    </p>
                  </div>
                </div>
                <div className="flex items-center gap-2">
                  <span className="font-black text-sm text-blue-400">-{alert.amount}€</span>
                  <ChevronRight className="w-4 h-4 text-gray-600 group-hover:text-emerald-500 transition-colors" />
                </div>
              </div>
            ))}
            <button className="w-full py-4 border-2 border-dashed border-[#30363D] rounded-3xl text-xs font-bold text-gray-500 hover:text-emerald-500 hover:border-emerald-500/50 transition-all flex items-center justify-center gap-2">
              <PlusCircle className="w-4 h-4" />
              CONFIGURAR NUEVO RECORDATORIO
            </button>
          </div>
        </section>

        {/* Actividad Reciente */}
        <section className="space-y-4">
          <h3 className="text-lg font-black tracking-tight px-1 text-gray-400 uppercase text-[10px]">Actividad Reciente</h3>
          <div className="bg-[#161B22]/80 backdrop-blur-md border border-[#30363D] rounded-[2rem] overflow-hidden">
            <div className="p-8 text-center space-y-2">
              <div className="w-12 h-12 bg-gray-800 rounded-full mx-auto flex items-center justify-center mb-4">
                <PlusCircle className="w-6 h-6 text-gray-600" />
              </div>
              <p className="text-sm font-bold text-gray-300">¿No hay nada por aquí?</p>
              <p className="text-xs text-gray-500 leading-relaxed">
                Empieza a registrar tus movimientos para que Moneypaz tome el control.
              </p>
            </div>
          </div>
        </section>
      </main>

      {/* Botón Flotante de Registro */}
      <button 
        onClick={() => setIsModalOpen(true)}
        className="fixed bottom-8 right-8 w-16 h-16 bg-emerald-500 text-black rounded-full shadow-2xl shadow-emerald-500/40 flex items-center justify-center hover:scale-110 active:scale-95 transition-all z-50"
      >
        <PlusCircle className="w-8 h-8" strokeWidth={3} />
      </button>

      {/* Modal Placeholder para Nuevo Registro */}
      {isModalOpen && (
        <div className="fixed inset-0 bg-black/90 backdrop-blur-sm z-[100] flex items-end sm:items-center justify-center p-4 animate-in fade-in duration-200">
          <div className="bg-[#161B22] w-full max-w-sm rounded-[2.5rem] border border-[#30363D] overflow-hidden shadow-2xl">
            <div className="p-6 border-b border-[#30363D] flex justify-between items-center">
              <h2 className="text-xl font-black tracking-tighter">NUEVO REGISTRO</h2>
              <button onClick={() => setIsModalOpen(false)} className="p-2 hover:bg-white/5 rounded-full transition-colors">
                <X className="w-6 h-6 text-gray-500" />
              </button>
            </div>
            <div className="p-8 space-y-6 text-center">
              <p className="text-gray-400 text-sm">El formulario de registro con la opción de recordatorio se está configurando...</p>
              <button 
                onClick={() => setIsModalOpen(false)}
                className="w-full py-4 bg-emerald-500 text-black font-black rounded-2xl hover:bg-emerald-400 transition-all"
              >
                ENTENDIDO
              </button>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
